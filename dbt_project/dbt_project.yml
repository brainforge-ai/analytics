name: 'dummyclient'
version: '1.0.0'
require-dbt-version: [">=1.0.0", "<2.0.0"]

profile: 'dummyclient'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dummyclient:
    +materialized: table   # everything defaults to table
